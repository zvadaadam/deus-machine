<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Network access (WebSocket, HTTP to backend, GitHub API) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- File access (workspace directories, git repos) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Allow spawning child processes (backend, sidecar, git, gh) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow JIT compilation (V8 in renderer and backend) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Disable library validation (for native modules like better-sqlite3) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
